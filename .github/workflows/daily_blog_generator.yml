name: Daily AI Blog Post Generation

on:
  schedule:
    # 한국 시간 08:10 (UTC 23:10)
    - cron: '10 23 * * *'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    env:
      TZ: 'Asia/Seoul'

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.11'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        # 🟢 [중요] 새로운 SDK(google-genai)와 pydantic 설치
        pip install google-genai pydantic requests

    - name: Run Blog Generator Script
      env:
        GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
      run: |
        # 파일명이 다르다면 여기서 수정해주세요 (예: main.py, blog_gen.py 등)
        python generate_blog_content.py

    - name: Commit and Push changes
      run: |
        git config --global user.name "github-actions[bot]"
        git config --global user.email "41898282+github-actions[bot]@users.noreply.github.com"
        git add _posts/ assets/images/
        if git diff --staged --quiet; then
          echo "No changes to commit."
        else
          git commit -m "🤖 AI Auto-Post: $(date +'%Y-%m-%d')"
          git push
        fi